import React from 'react';
import styles from './Header.css';

const Header = () => (
  <header className={styles.Header}>
    <h1>The First 10 Characters in the Rick and Morty API</h1>
  </header>
);

export default Header;
